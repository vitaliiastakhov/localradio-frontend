query Sitemap {
  mixes(pagination: { limit: -1 }) {
    data {
      attributes {
        slug
      }
    }
  }
  shows(pagination: { limit: -1 }) {
    data {
      attributes {
        slug
      }
    }
  }
  guests(pagination: { limit: -1 }) {
    data {
      attributes {
        slug
      }
    }
  }
  genres(pagination: { limit: -1 }) {
    data {
      attributes {
        slug
      }
    }
  }
  moods(pagination: { limit: -1 }) {
    data {
      attributes {
        slug
      }
    }
  }
  releases(pagination: { limit: -1 }) {
    data {
      attributes {
        slug
      }
    }
  }
  events(pagination: { limit: -1 }) {
    data {
      attributes {
        slug
      }
    }
  }
}
