import { TextInput } from './adapters';
import { createField } from './create-field';
import { createObjectValidator } from './create-validator';

const Field = {
  TextInput,
};

export { createField, createObjectValidator, Field };
